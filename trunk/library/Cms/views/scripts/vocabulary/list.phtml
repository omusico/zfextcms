<h3>Vocabulario</h3>

<? if (count($this->paginator)): ?>
<table class="fullwidth" border="0" cellpadding="0" cellspacing="0">
    <thead>
      <tr>
            <th >ID</th>
            <th >Titulo</th>
            <th></th>
      </tr>
    </thead>

    <tbody>
        <? $i=0; foreach ($this->paginator as $vocabulary) :  ?>
              <tr class=<?php echo ($i%2==0?'"even"':'"odd"') ?>>
                    <td valign="top"><?php echo $vocabulary['vocabulary_id']; ?></td>
                    <td valign="top"><?php echo $vocabulary['title'] ?></td>
                    <td valign="top">
						<?php echo $this->href( "vocabulary", "update", "editar vocabulario", array( "vid" => $vocabulary['vocabulary_id'] ) ) ?>
                        |
                        <?php echo $this->href( "taxonomy", "list", "listar terminos", array( "vid" => $vocabulary['vocabulary_id'] ) ) ?>
                        |
                        <?php echo $this->href( "taxonomy", "create", "agregar terminos", array( "vid" => $vocabulary['vocabulary_id'] ) ) ?>
						| <a href="#" onclick="confirmDelete( '/CmsPanel/vocabulary/delete/id/<?=$vocabulary['vocabulary_id']?>' );return false;" >Borrar</a>
  					</td>
              </tr>
        <? $i++; endforeach; ?>
    </tbody>
</table>
<?php echo $this->paginator ?>
<? endif; ?>
<p>
<?php echo $this->href( "vocabulary", "create", "Crear un vocabulario" ) ?>
</p>

